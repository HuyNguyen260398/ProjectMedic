@model IEnumerable<ProjectMedic.Models.Working_Day>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WorkingDay_Period)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WorkingDay_Period)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkingDay_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkingDay_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkingDay_ID })
        </td>
    </tr>
}

</table>
