@model IEnumerable<ProjectMedic.Models.Working_Time>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WoringTime_Period)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WoringTime_Period)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkingTime_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkingTime_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkingTime_ID })
        </td>
    </tr>
}

</table>
