@model IEnumerable<ProjectMedic.Models.Academic_Title>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AcademicTitle_Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AcademicTitle_Means)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AcademicTitle_Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AcademicTitle_Means)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AcademicTitle_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AcademicTitle_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AcademicTitle_ID })
        </td>
    </tr>
}

</table>
